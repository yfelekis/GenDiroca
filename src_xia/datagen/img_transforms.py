import torchvision.transforms as transforms


def get_transform(transform_name, img_size):
    # if transform_name == "mnist":
    #     return transforms.Compose([
    #         transforms.Resize((img_size, img_size)),
    #     ])
    # else:
    #     return None
    return None
